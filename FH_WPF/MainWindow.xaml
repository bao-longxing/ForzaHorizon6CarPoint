<Window x:Class="FH_WPF.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:FH_WPF"
        mc:Ignorable="d"
        Title="Horizon 6 脚本工具"
        MouseLeftButtonDown="Window_MouseLeftButtonDown"
        Closing="Window_Closing"
        Width="1024"
        Height="768"
        FontFamily="Segoe UI"
        WindowStartupLocation="CenterScreen"
        AllowsTransparency="True"
        Background="Transparent" WindowStyle="None">
    <Window.Resources>
        <SolidColorBrush x:Key="PanelBrush" Color="#101A2D"/>
        <SolidColorBrush x:Key="CardBrush" Color="#0E1727"/>
        <SolidColorBrush x:Key="BorderBrushDark" Color="#26334A"/>
        <SolidColorBrush x:Key="TextBrush" Color="#E4ECFF"/>
        <SolidColorBrush x:Key="SubTextBrush" Color="#8D9AB4"/>

        <Style x:Key="CardBorderStyle" TargetType="Border">
            <Setter Property="Background" Value="{StaticResource CardBrush}"/>
            <Setter Property="BorderBrush" Value="{StaticResource BorderBrushDark}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="CornerRadius" Value="8"/>
            <Setter Property="Padding" Value="12"/>
            <Setter Property="Margin" Value="0,0,0,10"/>
        </Style>

        <Style x:Key="SidebarButtonStyle" TargetType="Button">
            <Setter Property="Foreground" Value="{StaticResource TextBrush}"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderBrush" Value="Transparent"/>
            <Setter Property="HorizontalContentAlignment" Value="Left"/>
            <Setter Property="Padding" Value="12,8"/>
            <Setter Property="Margin" Value="8,2"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="root" Background="{TemplateBinding Background}" CornerRadius="6">
                            <ContentPresenter VerticalAlignment="Center" HorizontalAlignment="Left" Margin="6,0"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="root" Property="Background" Value="#1A2840"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="ActionButtonStyle" TargetType="Button">
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Background" Value="#5B3BE5"/>
            <Setter Property="BorderBrush" Value="#6E51F0"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="16,8"/>
            <Setter Property="Margin" Value="0,0,8,0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="6">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="SecondaryButtonStyle" TargetType="Button" BasedOn="{StaticResource ActionButtonStyle}">
            <Setter Property="Background" Value="#1B273B"/>
            <Setter Property="BorderBrush" Value="#32445F"/>
            <Setter Property="Foreground" Value="#CAD6F6"/>
        </Style>

        <Style x:Key="DangerButtonStyle" TargetType="Button" BasedOn="{StaticResource ActionButtonStyle}">
            <Setter Property="Background" Value="#7F1F2F"/>
            <Setter Property="BorderBrush" Value="#A9364D"/>
        </Style>

        <Style x:Key="GhostButtonStyle" TargetType="Button" BasedOn="{StaticResource ActionButtonStyle}">
            <Setter Property="Background" Value="#121D30"/>
            <Setter Property="BorderBrush" Value="#2A3B56"/>
            <Setter Property="Foreground" Value="#9FB2D6"/>
            <Setter Property="Padding" Value="10,6"/>
        </Style>

        <!-- 与 ActionButtonStyle 相同视觉风格但用于非 Button 控件（Label） -->
        <Style x:Key="ActionLabelStyle" TargetType="Label">
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Background" Value="#5B3BE5"/>
            <Setter Property="BorderBrush" Value="#6E51F0"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="16,8"/>
            <Setter Property="Margin" Value="0,0,8,0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="HorizontalContentAlignment" Value="Center"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Label">
                        <Border Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="6">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.Background>
            <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                <GradientStop Color="#050A14" Offset="0"/>
                <GradientStop Color="#09152A" Offset="0.5"/>
                <GradientStop Color="#040812" Offset="1"/>
            </LinearGradientBrush>
        </Grid.Background>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="0"/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>

        <Grid Grid.Column="1" Margin="12,52,12,12">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <Grid Grid.Row="1" Grid.RowSpan="2" Margin="0,0,0,10">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="250"/>
                    <ColumnDefinition Width="3*"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0" Margin="0,0,10,0">
                    <Border Style="{StaticResource CardBorderStyle}" Margin="0,0,0,0">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="120"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>

                            <TextBlock Text="🚗 配置信息" Grid.ColumnSpan="2" Foreground="{StaticResource TextBrush}" FontWeight="SemiBold" Margin="0,0,0,2"/>
                            <TextBlock Text="车辆性能分(用于删除)" Grid.Row="1" Foreground="{StaticResource SubTextBrush}" VerticalAlignment="Center"/>
                            <TextBox x:Name="txtCarScore" Grid.Row="1" Grid.Column="1" Height="34" Background="#0A1322" Foreground="#DDE8FF" BorderBrush="#2A3A56" Text="600" VerticalContentAlignment="Center"/>
                            <TextBlock Text="车辆厂商" Grid.Row="2" Foreground="{StaticResource SubTextBrush}" VerticalAlignment="Center"/>
                            <TextBox x:Name="txtCarFactory" Grid.Row="2" Grid.Column="1" Height="34" Background="#0A1322" Foreground="#DDE8FF" BorderBrush="#2A3A56" Text="斯巴鲁" UseLayoutRounding="False" VerticalContentAlignment="Center"/>
                            <TextBlock Text="车辆名称" Grid.Row="3" Foreground="{StaticResource SubTextBrush}" VerticalAlignment="Center" Margin="0,10,0,0"/>
                            <TextBox x:Name="txtCarType" Grid.Row="3" Grid.Column="1" Height="34" Background="#0A1322" Foreground="#DDE8FF" BorderBrush="#2A3A56" Text="22B" VerticalContentAlignment="Center"/>
                            <TextBlock Text="购买数量" Grid.Row="4" Foreground="{StaticResource SubTextBrush}" VerticalAlignment="Center"/>
                            <TextBox x:Name="txtBuyCarNum" Grid.Row="4" Grid.Column="1" Height="34" Background="#0A1322" Foreground="#DDE8FF" BorderBrush="#2A3A56" Text="33" VerticalContentAlignment="Center"/>
                            <TextBlock Text="蓝图代码" Grid.Row="5" Foreground="{StaticResource SubTextBrush}" VerticalAlignment="Center"/>
                            <TextBox x:Name="txtScriptCode" Grid.Row="5" Grid.Column="1" Height="34" Background="#0A1322" Foreground="#DDE8FF" BorderBrush="#2A3A56" Text="170516901" VerticalContentAlignment="Center"/>
                            <TextBlock Text="单局点数" Grid.Row="6" Foreground="{StaticResource SubTextBrush}" VerticalAlignment="Center"/>
                            <TextBox x:Name="txtSinglePoint" Grid.Row="6" Grid.Column="1" Height="34" Background="#0A1322" Foreground="#DDE8FF" BorderBrush="#2A3A56" Text="9" VerticalContentAlignment="Center"/>
                        </Grid>
                    </Border>

                    <Border Style="{StaticResource CardBorderStyle}" Margin="0,10,0,10" Padding="12,12,0,12">
                        <StackPanel>
                            <TextBlock Text="⌨ 快捷键提示" Foreground="{StaticResource TextBrush}" FontWeight="SemiBold" Margin="0,0,0,10"/>
                            <WrapPanel>
                                <Label x:Name="lblAuto" Content="💻全自动执行(F5)" Style="{StaticResource ActionLabelStyle}" Background="#FF3184AF" Foreground="White" Margin="0,0,8,4" Width="215"/>
                                <Label x:Name="lblBuyFromCollection" Content="🟢 从收集簿购买 (F6)" Style="{StaticResource ActionLabelStyle}" Background="#2E7D32" Foreground="White" Margin="0,0,8,4" Width="215"/>
                                <Label x:Name="lblPoint" Content="✨ 消耗点数 (F7)" Style="{StaticResource ActionLabelStyle}" Background="#DE7F23" BorderBrush="#F39A45" Margin="0,0,8,4" Width="215"/>
                                <Label x:Name="lblDelectCar" Content="🗑 从车库中移除 (F8)" Style="{StaticResource ActionLabelStyle}" Background="#7F1F2F" BorderBrush="#A9364D" Margin="0,0,8,4" Width="215"/>
                                <Label x:Name="lblGotoRace" Content="🧬 蓝图脚本 (F9)" Style="{StaticResource ActionLabelStyle}" Margin="0,0,8,8" Width="215"/>
                            </WrapPanel>
                        </StackPanel>
                    </Border>

                    <Border Style="{StaticResource CardBorderStyle}" Margin="0,15,0,0">
                        <StackPanel>
                            <TextBlock Text="📶 当前状态" Foreground="{StaticResource TextBrush}" FontWeight="SemiBold" Margin="0,0,0,10"/>
                            <TextBlock x:Name="txtOBSState" Text="OBS连接： ● 已连接" Foreground="#6AD37A" Margin="0,2" HorizontalAlignment="Center"/>
                            <TextBlock x:Name="txtPointTotal" Text="车辆热练度总数： --" Foreground="{StaticResource SubTextBrush}" Margin="0,2" HorizontalAlignment="Center"/>
                        </StackPanel>
                    </Border>
                </StackPanel>

                <Border Grid.Column="1" Style="{StaticResource CardBorderStyle}" Margin="0,0,0,0">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>

                        <TextBlock Text="📄 日志" Foreground="{StaticResource TextBrush}" FontWeight="SemiBold" Margin="0,0,0,10"/>
                        <ListView x:Name="lvLog" Grid.Row="1"
                                                  Background="#08101D"
                                                  BorderBrush="#22344F"
                                                  BorderThickness="1"
                                                  Foreground="#DDE8FF"
                                                  FontSize="13"
                                                  VirtualizingPanel.IsVirtualizing="True"
                                                  VirtualizingPanel.ScrollUnit="Pixel"
                                                  ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                                                  ScrollViewer.VerticalScrollBarVisibility="Hidden">
                            <ListView.Resources>
                                <!-- 去除列表项选中时的蓝色高亮，保持深色风格 -->
                                <Style TargetType="ListViewItem">
                                    <Setter Property="Padding" Value="0,2"/>
                                    <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                                    <Setter Property="Foreground" Value="#DDE8FF"/>
                                    <Setter Property="Background" Value="Transparent"/>
                                    <Setter Property="BorderThickness" Value="0"/>
                                    <Setter Property="Template">
                                        <Setter.Value>
                                            <ControlTemplate TargetType="ListViewItem">
                                                <Border x:Name="bd" Background="{TemplateBinding Background}" Padding="0,2">
                                                    <GridViewRowPresenter/>
                                                </Border>
                                                <ControlTemplate.Triggers>
                                                    <Trigger Property="IsMouseOver" Value="True">
                                                        <Setter TargetName="bd" Property="Background" Value="#0F1E35"/>
                                                    </Trigger>
                                                    <Trigger Property="IsSelected" Value="True">
                                                        <Setter TargetName="bd" Property="Background" Value="#12233F"/>
                                                    </Trigger>
                                                </ControlTemplate.Triggers>
                                            </ControlTemplate>
                                        </Setter.Value>
                                    </Setter>
                                </Style>
                                <!-- 列头样式 -->
                                <Style TargetType="GridViewColumnHeader">
                                    <Setter Property="Background" Value="#0B1625"/>
                                    <Setter Property="Foreground" Value="#8D9AB4"/>
                                    <Setter Property="BorderBrush" Value="#22344F"/>
                                    <Setter Property="BorderThickness" Value="0,0,0,1"/>
                                    <Setter Property="Padding" Value="8,6"/>
                                    <Setter Property="FontSize" Value="12"/>
                                    <Setter Property="HorizontalContentAlignment" Value="Center"/>
                                </Style>
                            </ListView.Resources>
                            <ListView.View>
                                <GridView>
                                    <GridViewColumn Header="时间" Width="145">
                                        <GridViewColumn.CellTemplate>
                                            <DataTemplate>
                                                <TextBlock Text="{Binding Time}" Foreground="#DDE8FF"
                                                           HorizontalAlignment="Center" TextAlignment="Center"/>
                                            </DataTemplate>
                                        </GridViewColumn.CellTemplate>
                                    </GridViewColumn>
                                    <GridViewColumn Header="级别" Width="64">
                                        <GridViewColumn.CellTemplate>
                                            <DataTemplate>
                                                <Border Background="{Binding LevelBackground}"
                                                        CornerRadius="3" Padding="6,1" HorizontalAlignment="Center">
                                                    <TextBlock Text="{Binding Level}" Foreground="White"
                                                               FontSize="11" FontWeight="SemiBold" TextAlignment="Center"/>
                                                </Border>
                                            </DataTemplate>
                                        </GridViewColumn.CellTemplate>
                                    </GridViewColumn>
                                    <GridViewColumn Header="模块" Width="80">
                                        <GridViewColumn.CellTemplate>
                                            <DataTemplate>
                                                <TextBlock Text="{Binding Module}" Foreground="#DDE8FF"
                                                           HorizontalAlignment="Center" TextAlignment="Center"/>
                                            </DataTemplate>
                                        </GridViewColumn.CellTemplate>
                                    </GridViewColumn>
                                    <GridViewColumn Header="内容" Width="428">
                                        <GridViewColumn.CellTemplate>
                                            <DataTemplate>
                                                <TextBlock Text="{Binding Content}" Foreground="#DDE8FF"
                                                           HorizontalAlignment="Center" TextAlignment="Center"/>
                                            </DataTemplate>
                                        </GridViewColumn.CellTemplate>
                                    </GridViewColumn>
                                </GridView>
                            </ListView.View>
                        </ListView>

                        <StackPanel Grid.Row="2" Margin="0,10,0,0">
                            <TextBlock Text="整体进度" Foreground="{StaticResource SubTextBrush}"/>
                            <ProgressBar x:Name="PrgPointTo999" Value="100" Height="10" Margin="0,4,0,0" Background="#162238" Foreground="#9B4DFF"/>
                            <DockPanel Margin="0,6,0,0">
                                <TextBlock x:Name="txtRaceTime" Text="已用时：00:00:00" Foreground="{StaticResource SubTextBrush}"/>
                                <TextBlock x:Name="txtRaceEstimatedTime" Text="预计用时：00:00:00" Foreground="{StaticResource SubTextBrush}" HorizontalAlignment="Right"/>
                            </DockPanel>
                        </StackPanel>
                    </Grid>
                </Border>
            </Grid>

            <Border Grid.Row="3" Style="{StaticResource CardBorderStyle}">
                <StackPanel>
                    <TextBlock Text="⚡ 快捷功能" Foreground="{StaticResource TextBrush}" FontWeight="SemiBold" Margin="0,0,0,10"/>
                    <WrapPanel>
                        <Button x:Name="btnOBSSettings" Content="OBS" Click="btnOBSSettings_Click" Style="{StaticResource SecondaryButtonStyle}" Width="120"/>
                        <Button Content="KeyBoard" Click="Button_Click_1" Style="{StaticResource SecondaryButtonStyle}" Width="120"/>
                        <Button x:Name="btnOCRTest" Content="OCR" Click="btnOCRTest_Click" Style="{StaticResource SecondaryButtonStyle}" Width="120"/>
                        <Button x:Name="btnTestBycar" Content="买车脚本测试" Style="{StaticResource SecondaryButtonStyle}" Width="140" Click="btnTestBycar_Click"/>
                        <Button x:Name="btnROI" Content="ROI" Style="{StaticResource SecondaryButtonStyle}" Width="120" Click="btnROI_Click"/>
                        <Button x:Name="btnMousePos" Content="MousePos" Style="{StaticResource SecondaryButtonStyle}" Width="140" Click="btnMousePos_Click"/>
                    </WrapPanel>
                </StackPanel>
            </Border>

            <StatusBar Grid.Row="4" Background="#08111F" Foreground="#8FA1C7" BorderBrush="#1E2B43" BorderThickness="1" Padding="8,4">
                <StatusBarItem x:Name="txtTime" Content="当前时间：2026-05-20 12:00:15"/>
                <Separator/>
                <StatusBarItem x:Name="txtRunTime" Content="运行时长：00:15:30"/>
                <Separator/>
                <StatusBarItem Content="作者：1282297544"/>
            </StatusBar>
            <Button x:Name="btnClose" Content="X" Style="{StaticResource SecondaryButtonStyle}" Margin="0,-40,0,52" Click="btnClose_Click" Height="30" HorizontalAlignment="Right" Width="30"/>
        </Grid>
        <Image x:Name="ImgTitelLog" OpacityMask="White" Source="pack://application:,,,/Resouce/Forza_Horizon_6_logo.png" Margin="19,11" Grid.Column="1" Width="210" Height="42" HorizontalAlignment="Left" VerticalAlignment="Top"/>
        <TextBlock Text="脚本工具  v1.0.0" Foreground="#9AA7C2" Margin="23,63,1134,689" Grid.Column="1"/>
    </Grid>
</Window>
